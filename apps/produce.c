#include <xinu.h>
#include <prodcons.h>

void producer(int count) {
  // TODO: implement the following:
  // Iterates from 0 to count, setting the value of the global variable 'n' each time
  // print produced value (new value of 'n'): "produced : 8"
}
